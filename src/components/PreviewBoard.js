import React from 'react';
import PreviewGrid from './PreviewGrid';

const PreviewBoard = () => {
    return (
        <div>
            <PreviewGrid/>
        </div>
    );
}

export default PreviewBoard;